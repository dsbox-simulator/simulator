[workspace]

members = [
    "dsbox",
    "libproto",
    "payload_derive",
    "json_rpc_fn", "dsbox-core",
]

resolver = "3"

default-members = ["dsbox"]

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
