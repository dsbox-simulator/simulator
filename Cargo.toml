[workspace]

members = [
    "dsbox",
    "libproto",
    "payload_derive",
    "json_rpc_fn",
]

resolver = "1"

default-members = ["dsbox"]

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
