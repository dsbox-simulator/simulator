[workspace]

members = [
    "dsbox-core",
    "dsbox-tauri",
    "dsbox",
    "libproto",
    "payload_derive",
]

resolver = "3"

default-members = ["dsbox"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
