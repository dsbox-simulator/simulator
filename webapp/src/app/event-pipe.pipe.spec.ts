import { EventPipePipe } from './event-pipe.pipe';

describe('EventPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EventPipePipe();
    expect(pipe).toBeTruthy();
  });
});
